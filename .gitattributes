# Auto detect text files and perform LF normalization
* text=auto

# Set Git LFS tracking for large files
# Uncomment these if you decide to use Git LFS for your data files
#data/*.csv filter=lfs diff=lfs merge=lfs -text
#data/*.xlsx filter=lfs diff=lfs merge=lfs -text
#models/*.pkl filter=lfs diff=lfs merge=lfs -text

# Explicitly declare text files to avoid EOL normalization issues
*.py text
*.md text
*.sh text eol=lf
*.txt text
*.yml text
*.yaml text
*.json text

# Binary files
*.pkl binary
*.png binary
*.jpg binary 